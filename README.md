# Introduction 
This package is a basic implementation of the checkmate algorithm, that generates expected scores open item responses based on the scores given to similar responses.

Can be installed with `remotes::install_github("Citolab/nlirp")`
